from setuptools import setup

setup(name="glin",
      version=0.1,
      description="Contains necessary functions to run the GLIn (Green Logistics Initiative() software",
      author="JacobsHack Group 9",
      packages=["glin"],
      install_requires=["pandas"])
